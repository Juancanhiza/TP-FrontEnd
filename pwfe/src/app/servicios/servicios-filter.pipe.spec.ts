import { ServiciosFilterPipe } from './servicios-filter.pipe';

describe('ServiciosFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ServiciosFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
